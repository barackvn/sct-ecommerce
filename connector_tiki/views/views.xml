<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="connector_tiki.list">
      <field name="name">connector_tiki list</field>
      <field name="model">connector_tiki.connector_tiki</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="connector_tiki.action_window">
      <field name="name">connector_tiki window</field>
      <field name="res_model">connector_tiki.connector_tiki</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="connector_tiki.action_server">
      <field name="name">connector_tiki server</field>
      <field name="model_id" ref="model_connector_tiki_connector_tiki"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="connector_tiki" id="connector_tiki.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="connector_tiki.menu_1" parent="connector_tiki.menu_root"/>
    <menuitem name="Menu 2" id="connector_tiki.menu_2" parent="connector_tiki.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="connector_tiki.menu_1_list" parent="connector_tiki.menu_1"
              action="connector_tiki.action_window"/>
    <menuitem name="Server to list" id="connector_tiki" parent="connector_tiki.menu_2"
              action="connector_tiki.action_server"/>
    -->
  </data>
</odoo>